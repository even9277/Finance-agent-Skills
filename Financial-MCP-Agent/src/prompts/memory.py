"""
短期记忆（STM）压缩提示词 · 版本 v1.0
最后修改：2026-05-28
修改记录：初始版本，从 src/agents/stm_nodes.py 中提取
          包含对话历史摘要、工具输出摘要 共2条提示词
"""

PROMPT_VERSION = "v1.0"

# ─────────────────────────────────────────────────────────────────────────────
# 对话历史压缩提示词（金融专版）
# 将多轮对话压缩为精炼摘要，保留金融关键信息供后续轮次引用
# ─────────────────────────────────────────────────────────────────────────────
SUMMARIZE_CONVERSATION_PROMPT = """
你是一个金融对话摘要助手。请将以下对话历史压缩成精炼摘要，
严格保留以下关键信息，不得遗漏：

必须保留：
- 用户提到的所有股票代码/公司名称
- 用户明确表达的投资偏好（风险偏好、持有期限、关注板块）
- 用户提出的具体投资问题和关键约束
- 已生成报告的核心结论（股票代码、买入/持有/卖出建议、关键数值）

可以省略：
- 打招呼、寒暄等无实质内容的对话
- 重复表达同一意思的内容
- analyst 分析过程的中间步骤（只保留最终结论）

输出格式：纯文字段落，300字以内，使用中文。
"""

# ─────────────────────────────────────────────────────────────────────────────
# 工具输出摘要提示词
# 将 analyst 工具调用结果压缩为简短摘要，避免 STM 窗口撑满
# ─────────────────────────────────────────────────────────────────────────────
TOOL_DIGEST_PROMPT = """
请将以下 analyst 工具输出提炼为摘要，严格控制在 200 字以内。
保留：最终投资结论、关键财务数值（PE/PB/ROE/营收增速等）、明确的风险提示。
删除：完整新闻正文、逐行财务表格、图表描述。
附上对应的 log 引用 ID 供审计追溯。
"""

__all__ = ["PROMPT_VERSION", "SUMMARIZE_CONVERSATION_PROMPT", "TOOL_DIGEST_PROMPT"]
